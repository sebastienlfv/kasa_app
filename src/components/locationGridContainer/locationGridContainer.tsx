import React from 'react';
import LocationCard from '../locationCard/locationCard';

const LocationGridContainer: React.FC = () => {
  return (
    <div className="LocationGrid-container">
      <LocationCard />
    </div>
  );
};

export default LocationGridContainer;